# ==========================================
# 核心框架
# ==========================================
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
python-multipart>=0.0.6          # FastAPI 處理 multipart/form-data (圖片上傳)

# ==========================================
# 網路請求 (打外部 API：TDX / Google / OpenWeatherMap)
# ==========================================
httpx>=0.24.1

# ==========================================
# 資料庫 (PostgreSQL + PostGIS)
# 靜態資料 — 使用者、公車站、捷運站、YouBike 站、個人/社群足跡
# ==========================================
asyncpg>=0.28.0
SQLAlchemy>=2.0.0
geoalchemy2>=0.14.0              # PostGIS 空間查詢支援

# ==========================================
# 快取 (Redis)
# 動態資料 — 公車即時到站、YouBike 車輛數、天氣快取
# ==========================================
redis>=5.0.0

# ==========================================
# 資料驗證 / 設定
# ==========================================
pydantic>=2.0.0
pydantic-settings>=2.0.0          # 讀 .env 變數
email-validator>=2.0.0            # 驗證 email 格式

# ==========================================
# 認證相關
# ==========================================
python-jose[cryptography]>=3.3.0  # JWT Token 簽發與驗證
passlib[bcrypt]>=1.7.4            # 密碼雜湊 (bcrypt)
